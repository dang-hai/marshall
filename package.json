{
  "name": "marshall",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter @marshall/desktop dev",
    "build": "bun run --filter @marshall/desktop build",
    "start": "bun run --filter @marshall/desktop start",
    "typecheck": "tsc --build"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
